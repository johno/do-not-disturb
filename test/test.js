'use strict';

var assert = require('assert');
var doNotDisturb = require('..');

describe('do-not-disturb', function() {

  it('should do something awesome', function() {
    assert.equal(doNotDisturb(), true);
  });
});
